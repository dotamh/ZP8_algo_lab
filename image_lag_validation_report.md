# 陷阱型 Image Lag 模型 — 验证报告

> 相机脉冲显影(image lag)的物理建模与实测验证。
> **状态:单物种 Hammerstein 模型已通过三组实测验证。**

---

## 1. 结论速览

| 项目 | 结论 |
|---|---|
| Lag 类型 | 陷阱型 image lag(charge-trapping),凹饱和填充 |
| 数学模型 | **单物种 Hammerstein**:静态非线性 ∘ 固定 LTI 释放核 |
| 释放核 `h(t)` | **固定形状,与信号强度无关** —— 已验证 |
| 验证结果 | 三组实测(29/45/212 DN)归一化拖尾重合,离散 RMS = **0.019** |
| 拖尾衰减 | 每触发约 ×0.42,5–6 个触发后进入噪声 |

---

## 2. 模型

### 2.1 公式

相机对一个 δ 脉冲(信号强度 `Q`)的脉冲响应:

$$
r(t;Q) = \underbrace{[\,Q - f(Q)\,]\cdot\delta(t{=}t_{\text{peak}})}_{\text{当下显影}}
\;+\; \underbrace{f(Q)\cdot h(t)}_{\text{陷阱拖尾 (lag)}}
$$

- `f(Q)`:被陷阱俘获的电荷量(变成 lag 的部分),只与 `Q` 有关。
- `h(t)`:释放核,**固定形状**,与 `Q` 无关 —— 本报告核心验证对象。
- `t`:触发个数;本数据峰值在第 47 触发。

### 2.2 数学归类

| 层级 | 名称 |
|---|---|
| 系统结构 | **Hammerstein 系统**(静态非线性 ∘ LTI 卷积) |
| 核结构 | 可分离 / 秩-1:`lag(t;Q) = f(Q)·h(t)` |
| 非线性项 `f(Q)` | 指数饱和(Mitscherlich)`N_t(1−e^{−Q/Q_c})` |
| 线性项 `h(t)` | 指数型衰减卷积核 |

### 2.3 物理图像(水桶比喻)

- `Q` = 倒进桶的水量(输入信号)
- `N_t` = 桶的容量(陷阱总数,固定上限)
- `f(Q)` = 桶里实际存住的水(被俘获电荷)
- `h(t)` = 漏水的节奏(释放核,固定,与水量无关)

---

## 3. 验证方法与结果

### 3.1 唯一可证伪论断

单物种 Hammerstein 模型给出一个**无参数、可直接证伪**的预测:

> 每个 DN 的脉冲响应,峰后拖尾各自归一化后,**形状应与 `Q` 无关、完全重合**。

因为拖尾 = `f(Q)·h(t)`,归一化除掉标量 `f(Q)`,只剩与 `Q` 无关的 `h(t)`。

### 3.2 实测数据(按触发个数,峰值在第 47)

| 触发# | 29 DN | 45 DN | 212 DN |
|---|---|---|---|
| 47(峰) | 12 | 21 | 160 |
| 48 | 5.5 | 9.0 | 24.0 |
| 49 | 2.5 | 3.5 | 10.0 |
| 50 | 1.5 | 2.0 | 5.0 |
| 51 | 1.0 | 1.2 | 3.0 |
| 52 | 0.6 | 0.8 | 2.0 |
| 53 | 0.4 | 0.5 | 1.2 |
| 54 | 0.3 | 0.4 | 0.8 |

测量条件:44 stage,pixelHG,PGAHG,1080 kHz,三组采样率相同。

### 3.3 归一化拖尾(除以峰后首点)

| 峰后 t | 29 DN | 45 DN | 212 DN | 平均 h(t) |
|---|---|---|---|---|
| 1 | 1.000 | 1.000 | 1.000 | 1.000 |
| 2 | 0.455 | 0.389 | 0.417 | 0.420 |
| 3 | 0.273 | 0.222 | 0.208 | 0.234 |
| 4 | 0.182 | 0.133 | 0.125 | 0.147 |
| 5 | 0.109 | 0.089 | 0.083 | 0.094 |
| 6 | 0.073 | 0.056 | 0.050 | 0.059 |
| 7 | 0.055 | 0.044 | 0.033 | 0.044 |

**三条曲线对平均 `h(t)` 的离散 RMS = 0.019。**

→ 归一化拖尾重合,形状与信号强度无关。
**单物种 Hammerstein 模型成立,释放核 `h(t)` 固定。**

(29 DN 略高于平均,在读图误差内;其峰值小、信噪比差,属正常涨落。)

### 3.4 lag 强度随信号变化

| Q (DN) | 峰值 | 拖尾首点 | 首点/峰值 | 峰后拖尾总和 |
|---|---|---|---|---|
| 29 | 12 | 5.5 | 45.8% | 11.8 |
| 45 | 21 | 9.0 | 42.9% | 17.4 |
| 212 | 160 | 24.0 | 15.0% | 46.0 |

lag 占比随信号增大单调下降(46% → 15%)——陷阱容量有限,
大信号下陷阱填满,被俘获比例被稀释。这是 `f(Q)` 凹饱和的体现。

---

## 4. 释放核 h(t)

由三组实测平均得到的固定释放核(峰后,归一化):

```
t (峰后):  1      2      3      4      5      6      7
h(t):      1.000  0.420  0.234  0.147  0.094  0.059  0.044
```

- 相邻比值约 0.42–0.56,接近指数衰减;
  半对数图可进一步判定单指数 vs 多指数(陷阱物种数)。
- 物理:陷阱释放服从一阶动力学,`h(t) ≈ (1/τ)e^{−t/τ}`,
  `τ` 只与温度、陷阱能级有关,与信号无关 —— 与"形状固定"的验证一致。

### "8pix vs 4pix" 宽度差异 = 阈值假象

实测标题中 29 DN 标"全宽 8pix"、212 DN 标"宽度 4pix",曾疑似物理拖尾变窄。
**已排除:** 采样率相同、归一化拖尾重合证明衰减速率一致。
差异来自宽度判定阈值——212 DN 峰值高(160),尾部 2–3 DN 相对峰值仅 1–2%,
被阈值判为"结束";29 DN 尾部 0.6 DN 占峰值 5%,仍可见。
**物理拖尾长度不随信号变。**

---

## 5. 填充曲线 f(Q)

`f(Q)` 控制拖尾的绝对幅度,形式为凹饱和:

$$
f(Q) = N_t\left(1 - e^{-Q/Q_c}\right)
$$

由另一组(9 点,峰值+次强)数据拟合:`N_t ≈ 38.9 DN`(俘获电荷上限),
`Q_c ≈ 57 DN`(误差较大,拐点区缺采样点)。

> 注意:9 点表与本报告三条完整波形读数口径不同,
> `f(Q)` 标定与三条曲线的绝对幅度未完全对齐(峰值点分配存在 ~10–15% 偏差)。
> 这不影响 §3 的形状验证(形状验证不依赖 `f(Q)`)。
> **待办:** 用统一口径(完整波形)的多点数据重新拟合 `f(Q)`,见 §6。

---

## 6. 待办

1. **统一口径重拟 `f(Q)`** —— 用完整波形(而非"峰值+次强")在多个 DN 下
   提取拖尾总量 `Σr(t>peak)`,对 `Q` 拟合 `N_t(1−e^{−Q/Q_c})`。
2. **补拐点区采样点**(60/75/90/110/140 DN)—— 把 `Q_c` 误差压下来,
   并确认 `f(Q)` 是指数饱和还是硬钳位 `min(kQ, N_t)`。
3. **半对数图定 `h(t)` 物种数** —— `log h(t)` vs `t`:直线=单指数(单物种),
   凸曲线=多指数(多物种)。

---

## 7. 与检测的关系(matched filter / GLRT)

- 模型是 Hammerstein:静态非线性 `f(Q)` + 固定 LTI 核 `h(t)`。
- `h(t)` 已验证固定 → matched filter 对幅度缩放免疫,**用固定 `h` 做模板依然最优**。
- `f(Q)` 是未知幅度 → 由 GLRT 的 ML 估计闭式吸收,
  吸收后 GLRT 统计量核心项 `hᵀC⁻¹x` 即 matched filter。
- 推荐:matched filter 做形状匹配与定位;幅度作为待估标量,
  事后查 `f(Q)` 反推真实信号 `Q`。
- 单物种(`h` 固定,已验证)→ 只需"matched filter + 幅度归一化"这个最简 GLRT。

---

## 附:一页速查

```
模型:    r(t;Q) = [Q−f(Q)]·δ(峰)  +  f(Q)·h(t)

f(Q) = N_t·(1−e^(−Q/Q_c))     N_t≈38.9 DN  (待统一口径重拟)
h(t) = [1, 0.42, 0.23, 0.15, 0.09, 0.06, 0.04]   固定, 已验证

验证:    三组归一化拖尾重合, 离散 RMS = 0.019
         -> 单物种 Hammerstein 成立, h(t) 与信号无关

已澄清:  "8pix vs 4pix" 是宽度阈值假象, 非物理变化

下一步:  1) 完整波形口径重拟 f(Q)
         2) 补 60-140 DN 拐点区采样
         3) 半对数图定 h(t) 单/多指数

配套脚本: model_validation.py  (形状验证, 无参数)
          trap_lag_fit.py      (f(Q) 拟合)
```

---

**配套文件**
- `model_validation.py` / `model_validation.png` — §3 形状验证
- `trap_lag_fit.py` — `f(Q)` 拟合脚本
- `image_lag_model.md` — 完整建模文档(早期版本)
